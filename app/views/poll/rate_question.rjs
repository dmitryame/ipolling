page.replace_html "buttons.question.#{@question.id}",  "#{@question.rating}"

page["buttons.question.#{@question.id}"].visual_effect :highlight, :startcolor => "#114411", :endcolor => "#ffff88"
                            
